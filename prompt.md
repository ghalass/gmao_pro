- Analyser le fichier schema.prisma afin d’identifier le modèle Role et ses relations.

- Parcourir l’ensemble du projet pour comprendre l’implémentation existante de la page de gestion des utilisateurs (Users) :

* structure des composants
* logique CRUD
* gestion d’état
* formulaires, validations et UI

- Créer une page complète de gestion des rôles en reproduisant exactement la même architecture et les mêmes bonnes pratiques utilisées pour les utilisateurs, en les adaptant au contexte des rôles.

- Assurer la cohérence avec :

* le style UI existant
* les conventions du projet
* la logique métier et les permissions liées aux rôles.

- s'assure que tout les endpoint traitent par rapport au entrepriseId
- Gère les relations entre les modèles (de tout type, ManyToOne, OneToMany, ManyToMany), implicite et explicite

<!-- TOUTES LES PAGES DE GESTION DES MODELES MANQUANT -->

- Analyser le fichier schema.prisma afin d’identifier le modèle Saisiehrm et ses relations.
- Parcourir l’ensemble du projet pour comprendre l’implémentation existante de la page de gestion des utilisateurs (Users) :

* structure des composants
* logique CRUD
* gestion d’état
* formulaires, validations et UI

- Créer une page complète de gestion des rôles en reproduisant exactement la même architecture et les mêmes bonnes pratiques utilisées pour les utilisateurs, en les adaptant au contexte des Saisiehrm.
- Assurer la cohérence avec :

* le style UI existant
* les conventions du projet
* la logique métier et les permissions liées aux Saisiehrm.

- s'assure que tout les endpoint traitent par rapport au entrepriseId
- Gère les relations entre les modèles (de tout type, ManyToOne, OneToMany, ManyToMany), implicite et explicite
